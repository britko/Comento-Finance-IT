create table stock_kosdaq
(
    code       text   not null COMMENT '종목코드',
    code_name  text   not null COMMENT '종목명',
    check_item int    null COMMENT '추가체크명'
) COMMENT '코스닥';

create index ix_stock_kosdaq_index
    on stock_kosdaq (`code`);
